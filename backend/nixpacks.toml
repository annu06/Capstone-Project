[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npx prisma generate --schema=src/prisma/schema.prisma",
  "npm run build"
]

[start]
cmd = "npx prisma migrate deploy --schema=src/prisma/schema.prisma && node dist/app.js"
